# Gmail MCP Server Configuration

# Path to the credentials.json file from Google Cloud Console
CREDENTIALS_FILE=credentials.json

# Path to the token.json file for storing user credentials
TOKEN_FILE=token.json

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_PATH=/mcp 